{
  "devDependencies": {
    "husky": "^9.0",
    "lint-staged": "^16.1",
    "prettier": "^3.0"
  },
  "scripts": {
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,css,md,yml}": "prettier --write",
    "*.{rb,rake,gemspec,ru}": "bin/standardrb --fix",
    "Gemfile": "bin/standardrb --fix"
  }
}
